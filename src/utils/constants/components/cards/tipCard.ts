import { SCREEN_SIZE } from "@/utils/constants";

const tipCardBorderRadiusObj = {
  SMALL: 10,
  MEDIUM: 15,
  LARGE: 20,
};

const tipCardBorderRadius = tipCardBorderRadiusObj[SCREEN_SIZE];

export { tipCardBorderRadius };
